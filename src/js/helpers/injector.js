import SVGInject from '@iconfu/svg-inject';

const options = {
    makeIdsUnique: false,
}

SVGInject(document.querySelector("#hamburuger-inject"),options);
SVGInject(document.querySelector("#factory-inject"),options);
SVGInject(document.querySelector("#github-icon"), options);
SVGInject(document.querySelector("#mail-icon"), options);
SVGInject(document.querySelector("#telephone-icon"), options);
